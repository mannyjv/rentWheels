/** @type {import('next').NextConfig} */
const nextConfig = {
  //configfure domains images app can source images from
  images: {
    domains: ["cdn.imagin.studio"],
  },
};

module.exports = nextConfig;
